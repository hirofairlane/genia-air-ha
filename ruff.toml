# Code-quality gate for the add-on (the legacy custom_component under
# _reference/ is archived and intentionally not linted).
target-version = "py311"
line-length = 100
extend-exclude = ["_reference", "scripts"]

[lint]
select = ["E4", "E7", "E9", "F", "I"]  # pyflakes + import sorting + syntax
